# scripts/

Utility scripts for building the book — e.g. rendering `handbook/` into a website or PDF — and for setting up local lab environments (spinning up a scratch cluster, seeding sample data, etc).
