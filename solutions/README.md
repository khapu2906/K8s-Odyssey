# solutions/

Answers to the exercises in [`challenges/`](../challenges/), kept in a separate tree on purpose so the challenge itself stays spoiler-free while reading.

One folder per chapter, e.g. `solutions/ch09/`.
