# mini-kubernetes/

A small Kubernetes clone written in Go — the hands-on project for **Part X — Under the Hood** (Chapters 51–63).

Independent from [`project/`](../project/) (AI Workspace). Built incrementally alongside the internals chapters: a mini API server, scheduler, controller, kubelet, and reconciliation loop, culminating in Chapter 63.

**Status:** not started.
